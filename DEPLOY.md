# TTS 文本阅读器 — 部署指南

## 项目概述

纯静态前端项目，无需后端，使用浏览器内置 Web Speech API 实现文本转语音。支持 Markdown 语法过滤、语速/音量/音调调节、剪贴板粘贴、进度跳转、关键词高亮与点击定位。

---

## 本地运行

### 方式一：Python 内置服务器（推荐）

```bash
python server.py
```

访问：http://localhost:8080/tts-reader.html

### 方式二：双击 HTML 文件

直接双击 `tts-reader.html` 即可在浏览器中打开（部分浏览器可能限制剪贴板 API，建议通过本地服务器访问）。

### 方式三：其他静态服务器

```bash
# Node.js
npx serve .

# PHP
php -S localhost:8080
```

---

## 线上部署（任选其一）

### 方案一：GitHub Pages（免费，推荐）

1. 在 GitHub 创建公开仓库（如 `tts-reader`）
2. 将项目文件推送到仓库：
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/YOUR_USERNAME/tts-reader.git
   git branch -M main
   git push -u origin main
   ```
3. 进入仓库 **Settings → Pages → Source**，选择 `main` 分支和 `/ (root)` 目录，保存
4. 访问：`https://YOUR_USERNAME.github.io/tts-reader/tts-reader.html`

### 方案二：Vercel（免费）

1. 登录 https://vercel.com
2. 点击 **Add New Project**，导入你的 GitHub 仓库
3. 保持默认设置，点击 **Deploy**
4. 部署完成后访问生成的域名

### 方案三：Netlify（免费）

1. 登录 https://netlify.com
2. 将项目文件夹拖拽到部署区域，或连接 GitHub 仓库
3. 部署完成后即可访问

### 方案四：Cloudflare Pages（免费）

1. 登录 https://pages.cloudflare.com
2. 连接 GitHub 仓库，保持默认构建设置
3. 自动部署并生成链接

---

## 文件说明

| 文件 | 用途 |
|------|------|
| `tts-reader.html` | 主页面，包含完整的 HTML / CSS / JS |
| `server.py` | Python 简易静态服务器 |
| `package.json` | 项目描述文件 |
| `DEPLOY.md` | 本部署文档 |

---

## 浏览器兼容性

| 浏览器 | 支持情况 |
|--------|----------|
| Chrome / Edge | ✅ 完整支持 |
| Firefox | ✅ 支持 |
| Safari | ✅ 支持 |
| 移动端浏览器 | ✅ 支持 |

> ⚠️ 语音列表取决于操作系统已安装的语音包。Windows 10/11 默认自带中文和英文语音。

---

## 功能清单

- ✅ Markdown 语法过滤（自动跳过 `#`、`**`、`[]()` 等符号）
- ✅ 语速 / 音量 / 音调滑块调节
- ✅ 浏览器语音合成引擎
- ✅ 文本高亮 & 句级定位
- ✅ 点击阅读区任意句子，从该处开始朗读
- ✅ 进度条点击跳转
- ✅ 剪贴板粘贴
- ✅ 设置自动保存到 localStorage
- ✅ 快捷键：`Ctrl+Enter` 朗读、`Ctrl+S` 停止、`Ctrl+P` 暂停/继续
- ✅ 响应式布局，适配手机与电脑

---

## 新增功能

### 📜 历史记录
- 点击"开始朗读"自动保存文本到左侧历史面板
- 支持切换历史记录、逐条删除、一键清空
- 最多保留 50 条记录，去重并按时间倒序

### 🎨 主题切换
- **纸质模式** — 模拟纸张的暖色调，适合白天阅读
- **深色模式** — 护眼夜间主题，低对比度蓝光
- 点击标题栏右侧圆点即可切换，自动记忆偏好

### ⏮️⏭️ 句子跳转
- 点击"上一句"/"下一句"按钮逐句跳转
- 跳转时自动从新位置继续朗读

### ⌨️ 增强快捷键
| 快捷键 | 功能 |
|--------|------|
| `空格` | 暂停/继续 |
| `←` | 跳到上一句 |
| `→` | 跳到下一句 |
| `↑` | 语速 +0.1 |
| `↓` | 语速 -0.1 |
| `Esc` | 退出极简模式 |

### 📊 字符数与进度
- 粘贴区右下角实时显示总字符数
- 进度区显示已读/总字符数和百分比（如 `300 / 1000  30%`）

### 💾 断点记忆
- 关闭浏览器后再次打开，自动恢复上次阅读进度
- 自动保存（每 5 秒 + 页面关闭时），7 天内有效
- 恢复时提示距离上次的时间

### 📖 极简阅读模式
- 点击"极简"按钮进入沉浸式阅读
- 隐藏所有控件，只保留文本和底部最小化播放条
- 底部条包含播放/暂停/停止按钮和进度显示

### 🎯 句子级定位
- 高亮精度从段落级提升至句子级
- 按 `。！？；.!?\n` 分割句子
- 点击任意句子或快捷键跳转均可精确到句
